node_modules
dist
.DS_Store
*.local
.env
.env.*
!.env.example

# Local tooling / CAD workspace (not needed for web deploy)
.venv-cad/
assets/
*.blend
*.blend1
*.fbx

# Source archives — runtime uses .glb / textures only
*.zip
**/*.snapshot*/

# Build cache
*.tsbuildinfo

# Unused huge maps (not referenced by the app)
public/textures/leafy_grass_*
